import { prisma } from "@/prisma/prisma";

export const getAccountByUserId = async(userId: string) => {

    try {

        return;
        
    } catch (error) {
        console.log(error)
        return null
    }
    
}
